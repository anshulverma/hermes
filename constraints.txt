# Pinned constraints for the server extra: reproducible deploy overlay.
# These exact versions are known to work together and satisfy the floor pins
# in pyproject.toml [project.optional-dependencies] server and dev extras.
# Used by fleet/Dockerfile.control-plane: pip install -e '.[server]' -c constraints.txt

fastapi==0.140.13
uvicorn==0.52.0
starlette==1.3.1
httpx==0.28.1
